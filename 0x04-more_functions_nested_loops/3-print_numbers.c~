#include "holberton.h"
/**
 * print_numbers - checks if character is a numberprint numbers fro; 0 to 9
 *
 * Return: 1 or 0
 */
void print_numbers(void)
{
char c;
int i;
for (i = 48; i <= 57; i++)
_putchar(c);
_putchar('\n');
}
