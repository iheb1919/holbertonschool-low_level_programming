#include "holberton.h"
/**
 * print_rev - function that prints string in reverse
 *
 * @s: input of the function
 */
void print_rev(char *s)
{
int i = 0;
while (*(s + i) != '\0')
{
i++;
}
for (i = j; i >= 0; i--)
{
_putchar(*(s + i));
}
_putchar('\n');
}
